{
  "rule": "check-apt",
  "format": true,
  "output_on_change": true,
  "schedule": "0 0 * * * *",
  "channel": "#alerts",
  "actions": [
    { "type": "hex-local", "command": "/usr/lib/nagios/plugins/check_apt" }
  ]
}
