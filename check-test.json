{
  "rule": "check-test",
  "format": true,
  "match": "check test",
  "output_on_change": true,
  "schedule": "0 * * * * *",
  "channel": "#alerts",
  "actions": [
    { "type": "hex-local", "command": "/usr/lib/nagios/plugins/check_procs -a xyz -c 1:"  }
  ]
}
