{
  "rule": "check-nginx",
  "format": true,
  "output_on_change": true,
  "schedule": "0 0 */1 * * *",
  "channel": "#alerts",
  "actions": [
    { "type": "hex-local", "command": "/usr/lib/nagios/plugins/check_procs -C nginx -c 1:" }
  ]
}
